---
import { getEntry } from "astro:content";
import DocLayout from "../layouts/DocLayout.astro";

const entry = await getEntry("docs", "index");
if (!entry) throw new Error("Missing docs index");
---

<DocLayout entry={entry} />
